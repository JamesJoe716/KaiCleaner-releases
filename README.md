# KaiCleaner Update Channel

This public repository hosts signed KaiCleaner release archives and the
Sparkle appcast consumed by the app's built-in update checker.

KaiCleaner source publication is handled separately. This update-channel
repository does not publish the application source tree and does not declare a
source-code license.
